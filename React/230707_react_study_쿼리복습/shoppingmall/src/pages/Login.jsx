import React from 'react'

import { Header } from '../components'

const Login = ( {title} ) => {
  return (
    <div>
        
        <Header title = {title}  />

    </div>
  )
}

export default Login